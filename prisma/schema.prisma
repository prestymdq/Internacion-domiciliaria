datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Role {
  SUPERADMIN
  ADMIN_TENANT
  COORDINACION
  DEPOSITO
  LOGISTICA
  PROFESIONAL
  FACTURACION
  AUDITOR
}

enum TenantStatus {
  TRIALING
  ACTIVE
  PAST_DUE
  SUSPENDED
  DELETED
}

enum PlanTier {
  STARTER
  PRO
  ENTERPRISE
}

enum EpisodeStatus {
  ACTIVE
  DISCHARGED
  CANCELLED
}

enum StockMovementType {
  IN
  OUT
  ADJUSTMENT
}

enum ApprovedOrderStatus {
  DRAFT
  APPROVED
  CANCELLED
}

enum PickListStatus {
  DRAFT
  FROZEN
  PACKED
}

enum DeliveryStatus {
  DRAFT
  PACKED
  IN_TRANSIT
  DELIVERED
  INCIDENT
  CLOSED
}

enum IncidentCause {
  SIN_STOCK
  CAMBIO_INDICACION
  RECHAZO_DOMICILIO
  INCUMPLIMIENTO
}

enum VisitStatus {
  SCHEDULED
  IN_PROGRESS
  COMPLETED
  MISSED
  CANCELLED
}

enum AuthorizationStatus {
  PENDING
  ACTIVE
  EXPIRED
  DENIED
  SUSPENDED
}

enum RequirementStatus {
  PENDING
  SUBMITTED
  APPROVED
  REJECTED
}

enum InvoiceStatus {
  DRAFT
  ISSUED
  PARTIAL
  PAID
  CANCELLED
}

model Tenant {
  id           String        @id @default(cuid())
  name         String
  slug         String        @unique
  status       TenantStatus  @default(TRIALING)
  plan         PlanTier      @default(STARTER)
  trialEndsAt  DateTime?
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt

  users        User[]
  patients     Patient[]
  episodes     Episode[]
  products     Product[]
  warehouses   Warehouse[]
  batches      Batch[]
  stockMovements StockMovement[]
  kitTemplates KitTemplate[]
  approvedOrders ApprovedOrder[]
  pickLists    PickList[]
  deliveries   Delivery[]
  incidents    Incident[]
  auditLogs    AuditLog[]
  sequences    Sequence[]
  payers       Payer[]
  payerPlans   PayerPlan[]
  payerRequirements PayerRequirement[]
  authorizations Authorization[]
  authorizationRequirements AuthorizationRequirement[]
  visits       Visit[]
  clinicalNotes ClinicalNote[]
  episodeCarePlans EpisodeCarePlan[]
  episodeWorkflowStages EpisodeWorkflowStage[]
  visitChecklistItems VisitChecklistItem[]
  clinicalAttachments ClinicalAttachment[]
  subscription TenantSubscription?
  policy       TenantPolicy?
  invoices     Invoice[]
  debitNotes   DebitNote[]
  payments     Payment[]
  billingTemplates BillingTemplate[]
  billingRules BillingRule[]

  @@index([status])
}

model User {
  id           String    @id @default(cuid())
  name         String?
  email        String    @unique
  emailVerified DateTime?
  image        String?
  passwordHash String
  role         Role
  tenantId     String?
  tenant       Tenant?   @relation(fields: [tenantId], references: [id])
  isActive     Boolean   @default(true)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  accounts     Account[]
  sessions     Session[]
  auditLogs    AuditLog[] @relation("AuditActor")
  stockMovementsCreated StockMovement[] @relation("StockMovementCreatedBy")
  deliveryEvidenceUploaded DeliveryEvidence[] @relation("DeliveryEvidenceUploadedBy")
  incidentsCreated Incident[] @relation("IncidentCreatedBy")
  visitsAssigned Visit[] @relation("VisitAssignedUser")
  visitsCreated Visit[] @relation("VisitCreatedBy")
  clinicalNotesAuthored ClinicalNote[] @relation("ClinicalNoteAuthor")
  visitChecklistCompleted VisitChecklistItem[] @relation("VisitChecklistCompletedBy")
  clinicalAttachmentsUploaded ClinicalAttachment[] @relation("ClinicalAttachmentUploadedBy")
  authorizationRequirementUploads AuthorizationRequirement[] @relation("AuthorizationRequirementUploadedBy")
  invoicesCreated Invoice[] @relation("InvoiceCreatedBy")
  debitNotesCreated DebitNote[] @relation("DebitNoteCreatedBy")
  paymentsCreated Payment[] @relation("PaymentCreatedBy")

  @@index([tenantId, role])
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model Sequence {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  key       String
  value     Int      @default(0)
  updatedAt DateTime @updatedAt

  @@unique([tenantId, key])
}

model Patient {
  id         String   @id @default(cuid())
  tenantId   String
  tenant     Tenant   @relation(fields: [tenantId], references: [id])
  firstName  String
  lastName   String
  dni        String
  birthDate  DateTime?
  sex        String?
  phone      String?
  address    String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  episodes   Episode[]
  approvedOrders ApprovedOrder[]
  visits     Visit[]
  clinicalNotes ClinicalNote[]
  clinicalAttachments ClinicalAttachment[]
  authorizations Authorization[]
  invoices Invoice[]

  @@unique([tenantId, dni])
  @@index([tenantId, lastName])
}

model Episode {
  id         String        @id @default(cuid())
  tenantId   String
  tenant     Tenant        @relation(fields: [tenantId], references: [id])
  patientId  String
  patient    Patient       @relation(fields: [patientId], references: [id])
  status     EpisodeStatus @default(ACTIVE)
  workflowStageId String?
  workflowStage EpisodeWorkflowStage? @relation(fields: [workflowStageId], references: [id])
  startDate  DateTime
  endDate    DateTime?
  diagnosis  String?
  notes      String?
  createdAt  DateTime      @default(now())
  updatedAt  DateTime      @updatedAt

  visits       Visit[]
  clinicalNotes ClinicalNote[]
  clinicalAttachments ClinicalAttachment[]
  carePlan    EpisodeCarePlan?
  authorizations Authorization[]

  @@index([tenantId, patientId])
}

model Product {
  id          String   @id @default(cuid())
  tenantId    String
  tenant      Tenant   @relation(fields: [tenantId], references: [id])
  name        String
  sku         String?
  unit        String
  packSize    Int?
  reorderPoint Int?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  batches     Batch[]
  stockMovements StockMovement[]
  kitItems    KitTemplateItem[]
  approvedOrderItems ApprovedOrderItem[]
  pickListItems PickListItem[]
  visitItems  VisitItem[]
  invoiceItems InvoiceItem[]
  billingRules BillingRule[]

  @@unique([tenantId, sku])
  @@index([tenantId, name])
}

model Warehouse {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  name      String
  location  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  stockMovements StockMovement[]
  visitItems  VisitItem[]
  pickListItems PickListItem[]

  @@unique([tenantId, name])
}

model Batch {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  productId String
  product   Product  @relation(fields: [productId], references: [id])
  code      String
  expiryDate DateTime?
  createdAt DateTime @default(now())

  stockMovements StockMovement[]

  @@unique([tenantId, productId, code])
}

model StockMovement {
  id          String            @id @default(cuid())
  tenantId    String
  tenant      Tenant            @relation(fields: [tenantId], references: [id])
  warehouseId String
  warehouse   Warehouse         @relation(fields: [warehouseId], references: [id])
  productId   String
  product     Product           @relation(fields: [productId], references: [id])
  batchId     String?
  batch       Batch?            @relation(fields: [batchId], references: [id])
  type        StockMovementType
  quantity    Int
  referenceType String?
  referenceId String?
  createdById String?
  createdBy   User?             @relation("StockMovementCreatedBy", fields: [createdById], references: [id])
  createdAt   DateTime          @default(now())

  @@index([tenantId, warehouseId])
  @@index([tenantId, productId])
}

model KitTemplate {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  items     KitTemplateItem[]

  @@unique([tenantId, name])
}

model KitTemplateItem {
  id           String      @id @default(cuid())
  kitTemplateId String
  kitTemplate  KitTemplate @relation(fields: [kitTemplateId], references: [id], onDelete: Cascade)
  productId    String
  product      Product     @relation(fields: [productId], references: [id])
  quantity     Int
}

model ApprovedOrder {
  id         String             @id @default(cuid())
  tenantId   String
  tenant     Tenant             @relation(fields: [tenantId], references: [id])
  patientId  String
  patient    Patient            @relation(fields: [patientId], references: [id])
  status     ApprovedOrderStatus @default(APPROVED)
  notes      String?
  createdAt  DateTime           @default(now())
  updatedAt  DateTime           @updatedAt

  items      ApprovedOrderItem[]
  pickLists  PickList[]
  deliveries Delivery[]

  @@index([tenantId, patientId])
}

model ApprovedOrderItem {
  id             String        @id @default(cuid())
  approvedOrderId String
  approvedOrder  ApprovedOrder @relation(fields: [approvedOrderId], references: [id], onDelete: Cascade)
  productId      String
  product        Product       @relation(fields: [productId], references: [id])
  quantity       Int
}

model PickList {
  id             String        @id @default(cuid())
  tenantId       String
  tenant         Tenant        @relation(fields: [tenantId], references: [id])
  approvedOrderId String
  approvedOrder  ApprovedOrder @relation(fields: [approvedOrderId], references: [id])
  status         PickListStatus @default(DRAFT)
  frozenAt       DateTime?
  packedAt       DateTime?
  stockCommittedAt DateTime?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt

  items          PickListItem[]
  deliveries     Delivery[]

  @@index([tenantId, status])
}

model PickListItem {
  id           String       @id @default(cuid())
  pickListId   String
  pickList     PickList     @relation(fields: [pickListId], references: [id], onDelete: Cascade)
  productId    String
  product      Product      @relation(fields: [productId], references: [id])
  warehouseId  String?
  warehouse    Warehouse?   @relation(fields: [warehouseId], references: [id])
  requestedQty Int
  pickedQty    Int
  incidentId   String?
  incident     Incident?    @relation(fields: [incidentId], references: [id])

  @@index([pickListId])
}

model Delivery {
  id             String        @id @default(cuid())
  tenantId       String
  tenant         Tenant        @relation(fields: [tenantId], references: [id])
  pickListId     String
  pickList       PickList      @relation(fields: [pickListId], references: [id])
  approvedOrderId String
  approvedOrder  ApprovedOrder @relation(fields: [approvedOrderId], references: [id])
  status         DeliveryStatus @default(DRAFT)
  deliveryNumber String
  scheduledAt    DateTime?
  inTransitAt    DateTime?
  deliveredAt    DateTime?
  closedAt       DateTime?

  carrierName    String?
  carrierDni     String?
  carrierSignedAt DateTime?
  receiverName   String?
  receiverDni    String?
  receiverRelation String?
  receiverSignedAt DateTime?

  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  evidence       DeliveryEvidence[]
  invoiceItems   InvoiceItem[]

  @@index([tenantId, status])
  @@unique([tenantId, deliveryNumber])
}

model DeliveryEvidence {
  id          String   @id @default(cuid())
  deliveryId  String
  delivery    Delivery @relation(fields: [deliveryId], references: [id], onDelete: Cascade)
  fileKey     String
  fileUrl     String?
  fileName    String
  mimeType    String
  size        Int
  uploadedById String?
  uploadedBy  User?    @relation("DeliveryEvidenceUploadedBy", fields: [uploadedById], references: [id])
  createdAt   DateTime @default(now())
}

model Incident {
  id          String        @id @default(cuid())
  tenantId    String
  tenant      Tenant        @relation(fields: [tenantId], references: [id])
  cause       IncidentCause
  description String?
  createdById String?
  createdBy   User?         @relation("IncidentCreatedBy", fields: [createdById], references: [id])
  createdAt   DateTime      @default(now())
  resolvedAt  DateTime?

  pickListItems PickListItem[]

  @@index([tenantId, cause])
}

model Payer {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  name      String
  code      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  plans       PayerPlan[]
  requirements PayerRequirement[]
  authorizations Authorization[]
  invoices Invoice[]
  billingTemplates BillingTemplate[]
  billingRules BillingRule[]

  @@unique([tenantId, name])
}

model PayerPlan {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  payerId   String
  payer     Payer    @relation(fields: [payerId], references: [id], onDelete: Cascade)
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  authorizations Authorization[]
  invoices Invoice[]
  billingRules BillingRule[]

  @@unique([tenantId, payerId, name])
}

model PayerRequirement {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  payerId   String
  payer     Payer    @relation(fields: [payerId], references: [id], onDelete: Cascade)
  name      String
  isRequired Boolean @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  authorizationRequirements AuthorizationRequirement[]

  @@index([tenantId, payerId])
}

model Authorization {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  payerId   String
  payer     Payer    @relation(fields: [payerId], references: [id])
  planId    String?
  plan      PayerPlan? @relation(fields: [planId], references: [id])
  patientId String
  patient   Patient  @relation(fields: [patientId], references: [id])
  episodeId String?
  episode   Episode? @relation(fields: [episodeId], references: [id])
  number    String
  status    AuthorizationStatus @default(PENDING)
  startDate DateTime
  endDate   DateTime?
  limitAmount Float?
  limitUnits Int?
  notes     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  requirements AuthorizationRequirement[]
  invoices     Invoice[]

  @@unique([tenantId, number])
  @@index([tenantId, payerId])
  @@index([tenantId, patientId])
}

model AuthorizationRequirement {
  id             String   @id @default(cuid())
  tenantId       String
  tenant         Tenant   @relation(fields: [tenantId], references: [id])
  authorizationId String
  authorization  Authorization @relation(fields: [authorizationId], references: [id], onDelete: Cascade)
  requirementId  String
  requirement    PayerRequirement @relation(fields: [requirementId], references: [id])
  status         RequirementStatus @default(PENDING)
  fileKey        String?
  fileUrl        String?
  fileName       String?
  mimeType       String?
  size           Int?
  uploadedById   String?
  uploadedBy     User?    @relation("AuthorizationRequirementUploadedBy", fields: [uploadedById], references: [id])
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([authorizationId])
  @@index([tenantId])
}

model TenantSubscription {
  id                     String   @id @default(cuid())
  tenantId               String   @unique
  tenant                 Tenant   @relation(fields: [tenantId], references: [id])
  stripeCustomerId       String?
  stripeSubscriptionId   String?
  stripePriceId          String?
  status                 String?
  currentPeriodEnd       DateTime?
  cancelAtPeriodEnd      Boolean  @default(false)
  trialEnd               DateTime?
  seatCount              Int?
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
}

model TenantPolicy {
  id                      String   @id @default(cuid())
  tenantId                String   @unique
  tenant                  Tenant   @relation(fields: [tenantId], references: [id])
  pastDueBlockedModules   Json?
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt
}

model Invoice {
  id            String        @id @default(cuid())
  tenantId      String
  tenant        Tenant        @relation(fields: [tenantId], references: [id])
  payerId       String
  payer         Payer         @relation(fields: [payerId], references: [id])
  planId        String?
  plan          PayerPlan?    @relation(fields: [planId], references: [id])
  patientId     String
  patient       Patient       @relation(fields: [patientId], references: [id])
  authorizationId String?
  authorization Authorization? @relation(fields: [authorizationId], references: [id])
  status        InvoiceStatus @default(ISSUED)
  invoiceNumber String
  currency      String        @default("ARS")
  issuedAt      DateTime      @default(now())
  dueDate       DateTime?
  totalAmount   Float         @default(0)
  notes         String?
  createdById   String?
  createdBy     User?         @relation("InvoiceCreatedBy", fields: [createdById], references: [id])
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt

  items        InvoiceItem[]
  debitNotes   DebitNote[]
  payments     Payment[]

  @@unique([tenantId, invoiceNumber])
  @@index([tenantId, payerId])
  @@index([tenantId, patientId])
  @@index([tenantId, authorizationId])
}

model InvoiceItem {
  id            String   @id @default(cuid())
  invoiceId     String
  invoice       Invoice  @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  deliveryId    String?
  delivery      Delivery? @relation(fields: [deliveryId], references: [id])
  productId     String
  product       Product  @relation(fields: [productId], references: [id])
  description   String?
  quantity      Int
  unitPrice     Float
  honorarium    Float    @default(0)
  total         Float
  evidenceCount Int      @default(0)
  evidenceMeta  Json?
  createdAt     DateTime @default(now())

  @@index([invoiceId])
}

model BillingRule {
  id          String   @id @default(cuid())
  tenantId    String
  tenant      Tenant   @relation(fields: [tenantId], references: [id])
  payerId     String
  payer       Payer    @relation(fields: [payerId], references: [id])
  planId      String?
  plan        PayerPlan? @relation(fields: [planId], references: [id])
  productId   String
  product     Product  @relation(fields: [productId], references: [id])
  unitPrice   Float
  honorarium  Float    @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([tenantId, payerId, planId])
  @@unique([tenantId, payerId, planId, productId])
}

model DebitNote {
  id          String   @id @default(cuid())
  tenantId    String
  tenant      Tenant   @relation(fields: [tenantId], references: [id])
  invoiceId   String
  invoice     Invoice  @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  amount      Float
  reason      String
  createdById String?
  createdBy   User?    @relation("DebitNoteCreatedBy", fields: [createdById], references: [id])
  createdAt   DateTime @default(now())

  @@index([tenantId, invoiceId])
}

model Payment {
  id          String   @id @default(cuid())
  tenantId    String
  tenant      Tenant   @relation(fields: [tenantId], references: [id])
  invoiceId   String
  invoice     Invoice  @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  amount      Float
  method      String?
  reference   String?
  paidAt      DateTime @default(now())
  createdById String?
  createdBy   User?    @relation("PaymentCreatedBy", fields: [createdById], references: [id])
  createdAt   DateTime @default(now())

  @@index([tenantId, invoiceId])
}

model BillingTemplate {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  payerId   String?
  payer     Payer?   @relation(fields: [payerId], references: [id])
  name      String
  config    Json
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([tenantId])
  @@index([tenantId, payerId])
  @@unique([tenantId, name])
}

model Visit {
  id           String   @id @default(cuid())
  tenantId     String
  tenant       Tenant   @relation(fields: [tenantId], references: [id])
  patientId    String
  patient      Patient  @relation(fields: [patientId], references: [id])
  episodeId    String?
  episode      Episode? @relation(fields: [episodeId], references: [id])
  assignedUserId String?
  assignedUser User?    @relation("VisitAssignedUser", fields: [assignedUserId], references: [id])
  createdById  String?
  createdBy    User?    @relation("VisitCreatedBy", fields: [createdById], references: [id])
  status       VisitStatus @default(SCHEDULED)
  scheduledAt  DateTime
  checkInAt    DateTime?
  checkOutAt   DateTime?
  notes        String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  clinicalNotes ClinicalNote[]
  items         VisitItem[]
  checklistItems VisitChecklistItem[]
  attachments  ClinicalAttachment[]

  @@index([tenantId, scheduledAt])
  @@index([tenantId, status])
}

model ClinicalNote {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  patientId String
  patient   Patient  @relation(fields: [patientId], references: [id])
  episodeId String?
  episode   Episode? @relation(fields: [episodeId], references: [id])
  visitId   String?
  visit     Visit?   @relation(fields: [visitId], references: [id], onDelete: Cascade)
  authorId  String
  author    User     @relation("ClinicalNoteAuthor", fields: [authorId], references: [id])
  content   String
  structured Json?
  createdAt DateTime @default(now())

  attachments ClinicalAttachment[]

  @@index([tenantId, patientId])
}

model VisitItem {
  id        String   @id @default(cuid())
  visitId   String
  visit     Visit    @relation(fields: [visitId], references: [id], onDelete: Cascade)
  productId String
  product   Product  @relation(fields: [productId], references: [id])
  warehouseId String?
  warehouse Warehouse? @relation(fields: [warehouseId], references: [id])
  quantity  Int
  createdAt DateTime @default(now())
}

model EpisodeCarePlan {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  episodeId String   @unique
  episode   Episode  @relation(fields: [episodeId], references: [id], onDelete: Cascade)
  summary   String?
  frequency String?
  objectives Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([tenantId, episodeId])
}

model EpisodeWorkflowStage {
  id        String   @id @default(cuid())
  tenantId  String
  tenant    Tenant   @relation(fields: [tenantId], references: [id])
  name      String
  sortOrder Int      @default(0)
  isTerminal Boolean @default(false)
  createdAt DateTime @default(now())

  episodes  Episode[]

  @@unique([tenantId, name])
  @@index([tenantId, sortOrder])
}

model VisitChecklistItem {
  id           String   @id @default(cuid())
  tenantId     String
  tenant       Tenant   @relation(fields: [tenantId], references: [id])
  visitId      String
  visit        Visit    @relation(fields: [visitId], references: [id], onDelete: Cascade)
  key          String
  label        String
  isRequired   Boolean  @default(true)
  isCompleted  Boolean  @default(false)
  notes        String?
  completedAt  DateTime?
  completedById String?
  completedBy  User?    @relation("VisitChecklistCompletedBy", fields: [completedById], references: [id])
  createdAt    DateTime @default(now())

  @@index([tenantId])
  @@index([visitId])
}

model ClinicalAttachment {
  id          String   @id @default(cuid())
  tenantId    String
  tenant      Tenant   @relation(fields: [tenantId], references: [id])
  patientId   String
  patient     Patient  @relation(fields: [patientId], references: [id])
  episodeId   String?
  episode     Episode? @relation(fields: [episodeId], references: [id])
  visitId     String?
  visit       Visit?   @relation(fields: [visitId], references: [id], onDelete: Cascade)
  noteId      String?
  note        ClinicalNote? @relation(fields: [noteId], references: [id], onDelete: Cascade)
  fileKey     String
  fileUrl     String?
  fileName    String
  mimeType    String
  size        Int
  uploadedById String?
  uploadedBy  User?    @relation("ClinicalAttachmentUploadedBy", fields: [uploadedById], references: [id])
  createdAt   DateTime @default(now())

  @@index([tenantId, patientId])
  @@index([visitId])
}

model AuditLog {
  id          String   @id @default(cuid())
  tenantId    String?
  tenant      Tenant?  @relation(fields: [tenantId], references: [id])
  actorId     String?
  actor       User?    @relation("AuditActor", fields: [actorId], references: [id])
  action      String
  entityType  String?
  entityId    String?
  meta        Json?
  ip          String?
  userAgent   String?
  createdAt   DateTime @default(now())

  @@index([tenantId, createdAt])
  @@index([actorId, createdAt])
}
